name: Apply Tạp hóa pending contract once
on:
  push:
    branches:
      - feature/finish-taphoa-sales-flow
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/finish-taphoa-sales-flow
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python tools/apply_taphoa_pending_contract_once.py
      - name: Commit pending contract fix
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add supabase/functions/getlink-orders/index.ts
          git diff --cached --quiet && exit 0
          git commit -m "fix: preserve pending order response contract"
          git push origin HEAD:feature/finish-taphoa-sales-flow
