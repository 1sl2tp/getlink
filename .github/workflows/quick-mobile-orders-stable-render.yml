name: Quick mobile orders stable render
permissions:
  contents: write
on:
  push:
    branches:
      - fix/mobile-orders-scroll-search
jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Patch stable order render
        run: python tools/patch_mobile_orders_stable_render.py
      - name: Stamp and verify focused behavior
        run: |
          python tools/stamp_static_build.py
          python tools/stamp_static_build.py --check
          python -m unittest tests.test_mobile_orders_scroll_search -v
          python -m unittest tests.test_order_list_detail_contract -v
          python -m unittest tests.test_taphoa_order_flow_polish -v
          python -m unittest tests.test_mobile_iframe_tap_fallback -v
          python -m unittest tests.test_mobile_interaction_owner -v
          python -m unittest tests.test_mobile_iframe_touch_scroll -v
          python -m unittest tests.test_mobile_standard_document_layout -v
          node --check order-management.js
          node --check taphoa-mobile-standard.js
      - name: Commit implementation and self-remove
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git rm -f .github/workflows/quick-mobile-orders-stable-render.yml tools/patch_mobile_orders_stable_render.py
          git add order-management.js tests/test_mobile_orders_scroll_search.py index.html version.json
          git commit -m 'fix: preserve mobile order search focus while filtering'
          git push origin HEAD:fix/mobile-orders-scroll-search
