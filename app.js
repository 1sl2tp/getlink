const $=s=>document.querySelector(s);const workflow="https://github.com/1sl2tp/getlink/actions/workflows/scrape.yml";$("#openAction").onclick=()=>{const url=$("#url").value.trim();if(!/^https?:\\/\\/(www\\.)?bachhoaxanh\\.com\\//i.test(url)){ $("#status").textContent="Vui lòng nhập URL bachhoaxanh.com.";return;}localStorage.setItem("bhx_url",url);$("#status").textContent="GitHub Actions đã mở. Chọn Run workflow và dán URL vào Category URL.";window.open(workflow,"_blank","noopener");};const saved=localStorage.getItem("bhx_url");if(saved)$("#url").value=saved;async function load(){try{const r=await fetch("data/products.json?ts="+Date.now());if(!r.ok)return;const d=await r.json(),rows=d.products||[];$("#count").textContent=`${rows.length} sản phẩm`;$("#tbody").innerHTML=rows.length?rows.map(p=>`<tr><td>${p.image?`<img src="${esc(p.image)}" loading="lazy">`:"—"}</td><td>${esc(p.name||"")}</td><td>${esc(p.price||"")}</td><td>${esc(p.packaging||"")}</td><td>${p.url?`<a href="${esc(p.url)}" target="_blank" rel="noopener">Mở</a>`:"—"}</td></tr>`).join(""):`<tr><td colspan="5" class="empty">Chưa có dữ liệu.</td></tr>`;}catch(e){}}function esc(x){return String(x??"").replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[m]));}load();